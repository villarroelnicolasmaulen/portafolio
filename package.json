{
  "name": "portafolionicovilla",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "vite": "^2.3.3"
  },
  "dependencies": {
    "gh-pages": "^3.2.0",
    "three": "^0.128.0"
  }
}
